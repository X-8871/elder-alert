# 近期未验收项统一验收报告

日期：2026-05-16

项目：基于 ESP32-S3 的独居老人异常状态风险预警与主动提醒终端

## 结论摘要

本报告统一整理 2026-05-14 至 2026-05-16 期间新增或变更后尚未完全闭环的功能项。

验收结论按证据等级分为三类：

- `通过`：已有实机、串口、云端公网、构建或用户确认等证据，足以作为当前阶段验收结论。
- `条件通过`：代码、构建、云端或本地流程已通过，但缺少 ESP32 实机端最终验证；当前只能作为阶段性验收。
- `待实机验收`：必须接硬件、烧录或抓串口/网页/PM2 日志后才能写成已通过。

重要说明：本报告不把未实测硬件功能写成“已通过实机验收”。这符合项目 `AGENTS.md` 中“证据不足不得写成事实”的规则。

## 当前统一构建验收

验收项：当前工作区固件整体构建

结论：通过

执行命令：

```powershell
C:\Espressif\python_env\idf5.5_py3.11_env\Scripts\python.exe C:\Users\22061\esp\v5.5.2\tools\idf.py build
```

结果：

- 构建通过。
- 已生成 `build/hello_world.bin`。
- 当前二进制大小：`0x1f8d90 bytes`。
- 最小 app 分区：`0x200000 bytes`。
- 剩余空间：`0x7270 bytes`，约 `1%`。

风险：

- app 分区已接近满载。后续继续加入图片、大字体、复杂 LVGL 控件或更多 PCM 语音资源前，应优先裁剪 LVGL/字体或调整分区表。

## 验收总表

| 模块 / 功能项 | 当前结论 | 证据 | 后续动作 |
|---|---|---|---|
| 域名 `spectator0618.online` 与备案页脚 | 通过 | ICP 备案号已上线；公网文件验证包含 `皖ICP备2026013632号-1` | 公安备案号下发后补页脚 |
| 网页 UI 收敛与备案展示 | 条件通过 | 本地截图验证、线上文件 grep 验证 | 用户常用浏览器手动点一遍登录/导航/调试 |
| 云端动态 TTS WAV 生成 | 通过 | `/api/speech/reply-audio` 公网返回 `audio/wav`，文件头 `RIFF...WAVE` | 继续验证 ESP32 下载与播放 |
| 固件域名 URL 替换 | 条件通过 | 三个源码 URL 已是 `http://spectator0618.online:3000/...`，ESP-IDF 构建通过 | 烧录后验证域名上报、录音上传、回复下载 |
| ESP32 动态 TTS 播放 | 待实机验收 | 服务器侧 WAV 已通过，ESP32 端尚无播放日志/听声证据 | 烧录后按 GPIO17 测试 |
| MAX98357A SD/EN 改 GPIO21 | 条件通过 | 代码和构建通过；GPIO4 冲突根因已明确 | 实物线必须从 GPIO4 改接 GPIO21 后复测 |
| ST7735 TFT + LVGL 显示链路 | 条件通过 | 代码接入、构建通过；接线已确认 | 接屏烧录，看方向、偏移、颜色、中文可读性 |
| TFT 显示代码整理 | 通过 | `DisplayController.c`、`TFT.c` 整理后构建通过 | 不需要单独实机，随 TFT 实机一起观察 |
| LD2410B UART bring-up | 通过 | 用户确认三项场景与手机 App 大致一致 | 继续验收主业务融合模式 |
| LD2410B 风险融合主循环 | 条件通过 | 代码接入 SensorHub/RiskEngine，构建通过 | 烧录后验证静坐/走动/离开三场景 |
| D6 代码小优化 | 条件通过 | JSON 转义、SOS 间歇、超时配置、FAULT 保护、token 机制均构建/本地验证 | 逐项补实机或公网证据 |
| MQ2 持续确认 | 条件通过 | 代码与构建通过 | 需安全条件下验证瞬时不报警、持续才报警 |
| 网页登录保护与调试页 | 条件通过 | 本地登录/API/截图验证；部分线上页面已验证 | 线上完整手动回归 |
| 语音模式 CARE / INTERACT / OFFLINE | 条件通过 | 代码、本地 API、构建验证；语义已同步 | 线上真实模式切换和 ESP32 喇叭联调 |

## 详细验收记录

### 1. 域名、备案与网页页脚

验收结论：通过

验收内容：

- 当前项目展示域名确认为 `spectator0618.online`。
- `spectator.online` 不是当前项目域名。
- ICP 备案号为 `皖ICP备2026013632号-1`。
- 首页和登录页已加入备案号页脚。

证据：

- `docs/ai_context/SESSION_LOG.md` 记录线上 `login.css?v=20260516-1736`、`styles.css?v=20260516-1736` 和备案号验证。
- `docs/ai_context/PROJECT_STATE.md` 记录 `https://spectator0618.online/api/session` 返回 JSON `200`。

未完成：

- 公安联网备案号尚未下发。

验收意见：

- 当前 ICP 展示要求已满足比赛和公网演示的基础要求。
- 公安备案号下发后，再补公安备案图标和链接。

### 2. 云端动态 TTS 音频生成

验收结论：通过

验收内容：

- 服务器端 `/api/speech/reply-audio` 能把最新 AI 回复生成 WAV 音频。

证据：

- 公网验证返回：
  - `Content-Type: audio/wav`
  - `Content-Length: 99884`
  - 文件头为 `RIFF...WAVE`
- `/api/status` 显示 ASR、AI Reply、TTS 配置可用。
- `/api/speech/latest` 有 AI 回复记录。

验收意见：

- 服务器侧“文本 AI 回复 -> TTS WAV”链路已经通过。
- 该结论不等于 ESP32 端已经能播放动态回复。

### 3. ESP32 动态 TTS 下载与播放

验收结论：待实机验收

验收内容：

- GPIO17 录音上传成功后，ESP32 请求 `http://spectator0618.online:3000/api/speech/reply-audio`。
- `SpeechReplyPlayer` 下载 WAV、解析 WAV、通过 MAX98357A 播放动态回复。
- 失败时回退播放本地固定语音“我听到了”。

已有证据：

- 服务器侧 WAV 已通过公网验证。
- 固件构建通过。
- 固件 URL 已改为 `http://spectator0618.online:3000/api/speech/reply-audio`。

缺失证据：

- ESP32 串口 HTTP 状态码。
- WAV 解析日志。
- I2S 播放日志。
- 实际听到动态 AI 回复的听声确认。

验收步骤：

1. 烧录当前固件。
2. 确认 MAX98357A `SD/EN` 已接 `GPIO21`。
3. 按 GPIO17 录音，说一句短句。
4. 观察串口是否出现动态音频下载成功。
5. 确认喇叭播放的是 AI 动态回复，而不是固定“我听到了”。

通过标准：

- 串口显示 `/api/speech/reply-audio` 返回 `200`。
- WAV 解析成功。
- MAX98357A 播放完成。
- 听感上能听到与网页 AI Reply 对应的动态回复。

失败时记录：

- ESP32 `SpeechReplyPlayer` HTTP 状态码。
- PM2 `[speech-tts]` 日志。
- 是否进入 `VoicePrompt` fallback。

### 4. 固件域名链路

验收结论：条件通过

验收内容：

固件源码当前三个云端地址已统一到 `spectator0618.online:3000`：

- 告警上报：`/api/alert`
- 语音上传：`/api/speech/transcribe`
- 回复音频下载：`/api/speech/reply-audio`

证据：

- `SOURCE_INDEX.md` 已记录三个模块当前 URL。
- `docs/ai_context/PROJECT_STATE.md` 已记录源码路径和 URL。
- 当前统一构建通过。

缺失证据：

- ESP32 实机通过域名上报 `NORMAL/SOS/REMIND/ALARM`。
- ESP32 实机通过域名上传 GPIO17 录音。
- ESP32 实机通过域名下载动态 TTS 音频。

验收意见：

- 代码和构建层面通过。
- 域名链路必须烧录后才可写成实机通过。

### 5. ST7735 TFT + LVGL 显示链路

验收结论：条件通过

验收内容：

- 使用 `1.8寸 ST7735 128x160 SPI TFT`。
- 接入 `LVGL`。
- 显示四种主状态：
  - `NORMAL`
  - `REMIND`
  - `ALARM`
  - `SOS`
- 离线使用灰色叠加提示，不作为第五状态。
- 底部保留 `确认 / SOS` 物理按键提示。

当前接线：

| TFT 引脚 | ESP32-S3 GPIO |
|---|---|
| CLK / SCL | GPIO40 |
| MOSI / SDA | GPIO41 |
| RST / RES | GPIO42 |
| DC | GPIO39 |
| CS | GPIO38 |
| BLK / LED | GPIO47 |
| VCC | 3V3 |
| GND | GND |

证据：

- `components/BSP/TFT/BSP_TFT.h`
- `components/BSP/TFT/TFT.c`
- `components/Middlewares/DisplayController/DisplayController.c`
- 当前统一构建通过。

缺失证据：

- 实物接屏后是否点亮。
- 方向是否正确。
- 是否有偏移、裁边、颜色异常。
- 中文字号在 `128x160` 屏上是否可读。

验收步骤：

1. 按上表接线。
2. 烧录当前固件。
3. 上电观察 TFT 是否点亮。
4. 分别触发或模拟 `NORMAL / REMIND / ALARM / SOS`。
5. 断网或未联网时确认灰色离线提示出现。

通过标准：

- 屏幕点亮且无花屏。
- 四态背景色符合：绿色、橙色、红色、深红。
- 顶部时间/联网状态可见。
- 底部 `确认 / SOS` 提示可见。
- 无明显镜像、偏移或裁边。

### 6. TFT / LVGL 代码整理

验收结论：通过

验收内容：

- `DisplayController.c` 做模块内部整理：
  - 提取 UI 常量。
  - 合并状态文案/颜色映射。
  - 拆分顶部栏、状态标签、按钮行创建逻辑。
- `TFT.c` 做模块内部整理：
  - 拆分 SPI、panel IO、panel driver、启动序列和日志。
  - 移除未使用内部状态变量。

证据：

- 整理后构建通过。
- 未改公开接口。
- 未改 GPIO、颜色、状态、接口、时序或 LVGL 服务入口。

验收意见：

- 该项属于代码结构验收，不需要单独硬件实测。
- 后续真实显示效果随 ST7735 实机验收一起观察。

### 7. MAX98357A SD/EN 改 GPIO21

验收结论：条件通过

验收内容：

- 解决 MAX98357A `SD/EN` 使用 `GPIO4` 导致 I2C SDA 冲突的问题。
- 代码已改为 `GPIO21`。
- `app_main()` 早期拉低 GPIO21，降低空闲噪声风险。

证据：

- `SESSION_LOG.md` 记录 BH1750/OLED I2C timeout 与 GPIO4 冲突。
- `SOURCE_INDEX.md` 记录 `MAX98357A` 当前 `SD/EN=GPIO21`。
- ESP-IDF 构建通过。

缺失证据：

- 实物线是否已从 GPIO4 改到 GPIO21。
- 改线后 I2C 是否恢复稳定。
- 喇叭嗡嗡声是否消失。

验收步骤：

1. 确认 MAX98357A `SD/EN` 实物线接 `GPIO21`。
2. 烧录当前固件。
3. 上电观察 I2C 传感器和显示是否正常初始化。
4. 按 GPIO17 播放语音，听播放前后是否仍有明显嗡嗡声。

通过标准：

- 串口不再出现 BH1750/OLED I2C timeout 连续报错。
- 播放前后无明显异常嗡嗡声。
- GPIO17 录音/播放后仍可再次录音。
- GPIO8 SOS 不受影响。

### 8. LD2410B 主业务融合

验收结论：条件通过

已通过部分：

- LD2410B UART bring-up 已通过。
- 用户确认离开探测区、静坐不动、走动/挥手三项与手机 App 大致一致。
- ESP32 串口曾输出 `presence/state/moving_cm/stationary_cm/energy`。

待验收部分：

- LD2410B 接入 `SensorHub` 和 `RiskEngine` 后的主业务融合模式。

当前融合逻辑：

- LD2410B 有效时：
  - 有人存在且长时间无明显运动 -> REMIND。
  - 离开探测区不应被当成“人在但静止”。
- LD2410B 无效时：
  - 回退 AM312 长时间无活动判断。

验收步骤：

1. 烧录当前集成固件。
2. 站在探测区内走动或挥手，确认不会立即进入 REMIND。
3. 静坐不动超过演示阈值，确认进入 REMIND。
4. 离开探测区，确认不会因“无人”被误判为“人在但静止”。
5. REMIND 后 15 秒不确认，确认升级 ALARM。
6. 按确认键解除。

通过标准：

- 三个场景均符合预期。
- GPIO8 SOS 和 GPIO17 录音仍可用。
- 串口无持续 UART 解析异常。

### 9. D6 小优化回归

验收结论：条件通过

已完成并构建通过：

- `HttpAlertReporter` JSON reason 转义。
- SOS 蜂鸣器 120ms 高频间歇。
- REMIND 超时 DEMO/REAL 配置化。
- 传感器健康/FAULT 最小展示与风险保护。
- 云端最小 token 鉴权机制。
- 旧 LED BSP GPIO4 隐患清理。

待补实机/公网证据：

| 项目 | 待验证内容 |
|---|---|
| JSON reason 转义 | 特殊字符 reason 上报后服务器可正确解析 |
| SOS 间歇蜂鸣 | 实际听感和 LED 节奏是否合适 |
| REMIND 超时配置 | DEMO 模式串口出现 `timeout_ms=15000` |
| 传感器健康/FAULT | 拔插传感器时显示/日志/风险保护符合预期 |
| token 鉴权 | 真实 token 部署后，公网错误 token 401、正确 token 200 |
| 旧 LED BSP 清理 | I2C/OLED/传感器与 LED/蜂鸣器实机回归正常 |

验收意见：

- 这些项已经完成“代码 + 构建验收”。
- 若用于答辩，可以表述为“已完成工程加固并通过构建验证，部分硬件回归待现场补测”。

### 10. MQ2 持续确认

验收结论：条件通过

已完成：

- MQ2 从单次 ADC 超阈值改为滑动平均 + 持续确认。
- 默认持续确认时间为 `3000ms`。
- 构建通过。

缺失证据：

- 瞬时升高不会立即进入 ALARM。
- 持续超过确认时间后进入 ALARM。
- 串口能观察 `mq2_filtered_raw` 符合预期。

安全注意：

- 不建议用明火或危险气体测试。
- 可在安全条件下用合规烟雾/酒精挥发，并保持通风。
- 若临时降低阈值做演示，必须声明是演示阈值，不得写成真实安全阈值。

### 11. 网页登录、导航与调试面板

验收结论：条件通过

已完成：

- 登录页。
- 会话 Cookie。
- 退出登录。
- 主要 API 登录保护。
- 左侧导航专注视图。
- 调试页集中语音识别、AI 回复、回复音频测试。
- 首页和登录页备案页脚。

已有证据：

- 本地 `node --check`。
- 本地登录/API 流程验证。
- 本地截图验证。
- 部分线上文件内容和备案号验证。

缺失证据：

- 用户常用浏览器完整手动点击回归。
- 线上登录后导航、模式切换、语音调试、退出登录全流程截图或确认。

验收步骤：

1. 打开 `https://spectator0618.online/`。
2. 确认未登录跳转登录页。
3. 登录后进入主面板。
4. 点击 `状态 / 数据 / 事件 / 模式 / 调试`。
5. 切换 `CARE / INTERACT`。
6. 播放回复音频测试。
7. 退出登录。

通过标准：

- 页面无空白、无 JS 报错。
- 导航切换正确。
- API 数据可刷新。
- 退出后不可继续访问主面板。

## 最小实机补测清单

若希望把本报告中所有 `条件通过` 和 `待实机验收` 变成 `通过`，建议按以下顺序一次性补测：

1. 接好 MAX98357A `SD/EN=GPIO21`。
2. 接好 ST7735：
   - `CLK=GPIO40`
   - `MOSI=GPIO41`
   - `RST=GPIO42`
   - `DC=GPIO39`
   - `CS=GPIO38`
   - `BLK=GPIO47`
3. 烧录当前 `build/hello_world.bin`。
4. 上电确认：
   - I2C 传感器无连续 timeout。
   - TFT 点亮，方向和颜色正常。
   - 主界面中文可读。
5. 按 GPIO8 测 SOS：
   - 声光提醒。
   - 云端上报。
   - 确认键解除。
6. 触发 REMIND：
   - 静坐/无活动进入 REMIND。
   - 15 秒未确认升级 ALARM。
   - 确认键解除。
7. 按 GPIO17 测语音：
   - 录音上传成功。
   - 网页显示 ASR 文本。
   - AI Reply 出现。
   - ESP32 下载 `/api/speech/reply-audio`。
   - 喇叭播放动态回复。
8. 断网/未联网观察：
   - TFT 灰色离线提示。
   - 本地 SOS/确认仍可用。

## 最终验收结论

截至 2026-05-16，本项目近期新增功能的工程状态为：

- 云端动态 TTS 生成：通过。
- 当前固件统一构建：通过。
- TFT/LVGL 代码接入：构建通过，待实机显示验收。
- ESP32 动态 TTS 播放：待实机验收。
- 域名 + 3000 固件链路：构建通过，待烧录实机验收。
- MAX98357A GPIO21 修复：代码通过，待改线后实机验收。
- LD2410B 主业务融合：代码通过，待烧录实机验收。
- D6 小优化：代码/构建通过，部分待硬件或公网回归。

可用于答辩的保守表述：

> 系统主闭环、云端展示、按键录音、固定语音提示和云端 TTS 生成已完成阶段性验收；近期新增的 TFT/LVGL 显示、动态 TTS 播放、域名链路和 LD2410B 主业务融合已完成代码接入与构建验证，正在进行实机回归验收。
